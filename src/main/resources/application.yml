server:
  port: 8080

# Actuator: Enable health and info endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

info:
  app:
    name: Simple API
    description: A minimal hello API with health check
    version: 1.0.0